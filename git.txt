git config --global user.name "babiswas"
git config --global user.email "biswas0814@gmail.com"
git config --list

git config user.email
git help

pwd to get the present working directory

cd .. to go back
cd folder/

Converting a folder into a git project.

git init

To see the hidden stuff 
ls -la

clear to clear the screen.

To add the current changes git add .

git add .

git commit -m "this is my first commit"

git log  # to see the activities in the repository.

git status

git diff

To check the difference.

git diff --staged

git rm hello1.txt

git log --author="babiswas"

git mv test.txt text1.txt

git mv test1.txt new_project


Pushing the project to github.

git remote add pro url

git commit pro master






